import React, {FC, PropsWithChildren} from 'react';

interface IProps extends PropsWithChildren {
}

const StarRating: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export {StarRating};